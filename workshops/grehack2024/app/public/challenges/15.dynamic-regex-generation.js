await import("/challenges/checkSolve.js");

// I had no way to properly ensure that you are using the "exec:" directive to solve this challenge.
// So simply using it to pop an alert should be enough for now :D
console.log("alert() with exec:");