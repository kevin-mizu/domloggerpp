{
  "hooks": {
    "PSTMSG": {
      "function": ["postMessage"],
      "event": ["message"]
    }
  },
  "config": {
    "postMessage": {
      "hookFunction": "$data=domlogger.utils.stringify(args[0]); $banwords=domlogger.hooksConfig?.['*']?.['!match']||[]; if (!banwords.some(k => $data.includes(k))) { console.log(`%c?%c→%c${domlogger.utils.getWindowContext(thisArg)}%c→%c${args[1]||'null'}%c ${$data}`, 'color: red', '', 'color: green', '', 'color: blue', ''); } return args;"
    },
    "message": {
      "alert": {
        "match": [
          ".match",
          ".indexOf",
          ".endsWith",
          ".includes",
          ".replace",
          ".startsWith",
          ".innerHTML",
          ".outerHTML",
          "postMessage",
          "opener",
          "eval"
        ],
        "notification": true
      }
    }
  }
}
